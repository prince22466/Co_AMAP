# this py script is to check each subfolder
# has the onnx file it is supposed to have
# aka, effective submission with score
# and print out the missing onnx which has effective score
# this script should check the file name info
# in files like 05_fill_additive_marking_local_3x3.md in submission_records,
# which tells what task submission is solved(aka, effective submission)
# should report if there are duplicates, 

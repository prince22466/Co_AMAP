apiVersion: batch/v1
kind: Job
metadata:
  name: trend-worker
  namespace: trend-platform
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: worker
          image: REGION-docker.pkg.dev/PROJECT/REPO/trend-worker:latest

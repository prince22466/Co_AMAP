name: forecast-event-test

on:
  push:
    branches:
      - test/v20-1-forecast-events

permissions:
  contents: read

jobs:
  forecast-events:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Run one-history forecast event suite
        run: |
          python Co_Kaggle/g5/local_arena/static_reply/forecast_tests/test_v20_1_history_events.py -v
